# Naval Strike — environment variables
# deploy.sh creates this file interactively on first run.
# Edit and re-run deploy.sh to change ports.

# Port exposed by the frontend Nginx container
FRONTEND_PORT=8080

# Port exposed by the WebSocket relay server
WS_PORT=3001

# WebSocket path (must match your reverse proxy config — do not change)
WS_PATH=/ws
