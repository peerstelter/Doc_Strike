# Naval Strike — environment variables
# Copy to .env and adjust before running docker compose

# WebSocket server port (must match docker-compose.yml)
PORT=3001

# WebSocket path (must match your reverse proxy config)
# Use /ws when behind a reverse proxy, leave empty for direct connections
WS_PATH=/ws
